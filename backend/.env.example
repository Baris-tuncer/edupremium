# ===========================================
# EDUPREMIUM BACKEND - ENVIRONMENT VARIABLES
# ===========================================
# Railway'de Variables sekmesine bu değerleri ekleyin

# Database (Railway Postgres'ten kopyala)
DATABASE_URL=postgresql://postgres:PASSWORD@postgres.railway.internal:5432/railway

# JWT Ayarları
JWT_SECRET=edupremium-jwt-secret-2024
JWT_REFRESH_SECRET=edupremium-refresh-secret-2024

# ===========================================
# RAILWAY KURULUM ADIMLARI
# ===========================================
# 1. + Create > Database > PostgreSQL ekle
# 2. edupremium > Settings > Root Directory = backend
# 3. edupremium > Variables > Yukarıdaki 3 değişkeni ekle
# 4. Deploy et
# ===========================================
